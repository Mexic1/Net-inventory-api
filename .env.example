# Copy to .env and adjust. Never commit .env.
POSTGRES_USER=inventory
POSTGRES_PASSWORD=change-me
POSTGRES_DB=inventory

# The API reads this one. Host is the compose service name.
DATABASE_URL=postgresql+psycopg://inventory:change-me@db:5432/inventory
